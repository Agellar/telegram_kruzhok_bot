{$DOMAIN} {
	encode gzip

	# Админка статистики — всё под /admin*
	@admin path /admin /admin/*
	handle @admin {
		reverse_proxy admin:8000
	}

	# Остальное (включая корень) — редирект на админку
	handle {
		redir https://{host}/admin 302
	}
}
