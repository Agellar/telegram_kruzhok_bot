services:
  bot:
    build: .
    container_name: telegram_kruzhok_bot
    restart: unless-stopped
    env_file: .env
    environment:
      STATS_DB: /data/stats.db
    volumes:
      - botdata:/data
      - /tmp/videobot:/tmp/videobot
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  admin:
    build: ./admin
    container_name: kruzhok_admin
    restart: unless-stopped
    env_file: .env
    environment:
      STATS_DB: /data/stats.db
    volumes:
      # rw-монтирование нужно для SQLite WAL (-shm/-wal sidecar-файлы),
      # сама админка открывает БД в режиме query_only=ON и только читает.
      - botdata:/data
    expose:
      - "8000"

  caddy:
    image: caddy:2
    container_name: kruzhok_caddy
    restart: unless-stopped
    depends_on:
      - admin
    ports:
      - "80:80"
      - "443:443"
    environment:
      DOMAIN: "${DOMAIN}"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config

volumes:
  botdata:
  caddy_data:
  caddy_config:
